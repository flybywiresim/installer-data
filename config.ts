import { Configuration } from "./lib";

const config: Configuration = {
    version: 1,
    publishers: [
        {
            name: 'FBW',
            key: 'flybywiresim',
            logoUrl: 'BRUH',
            addons: [],
        }
    ],
}

export default config;
